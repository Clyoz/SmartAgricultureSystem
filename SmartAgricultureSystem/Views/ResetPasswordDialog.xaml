<Window x:Class="SmartAgricultureSystem.Views.ResetPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重置密码" Height="250" Width="350"
        Background="#1E1E2E" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="#CDD6F4" FontSize="14" Margin="0,0,0,10">
            <Run Text="为用户 "/><Run x:Name="RunUsername" FontWeight="Bold" Foreground="#F9E2AF"/>
            <Run Text=" 重置密码"/>
        </TextBlock>

        <TextBlock Grid.Row="1" Text="新密码:" Foreground="#CDD6F4" Margin="0,5"/>
        <PasswordBox Grid.Row="2" x:Name="TxtNewPassword" Background="#313244" Foreground="White" Padding="5" Margin="0,2"/>

        <TextBlock Grid.Row="3" Text="确认密码:" Foreground="#CDD6F4" Margin="0,8,0,5"/>
        <PasswordBox Grid.Row="3" x:Name="TxtConfirmPassword" Background="#313244" Foreground="White" Padding="5" Margin="0,28,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="确定" Click="BtnConfirm_Click" Width="80" Margin="5,0"
                    Background="#A6E3A1" Foreground="#1E1E2E" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="取消" Click="BtnCancel_Click" Width="80" Margin="5,0"
                    Background="#45475A" Foreground="#CDD6F4" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
